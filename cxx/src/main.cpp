#include <iostream>
#include "lexer/Lexer.hpp"

// Phase 1 stub — lexer is implemented; parser and evaluator not yet written.
// This will be replaced in subsequent phases.
int main(int /*argc*/, char* /*argv*/[]) {
    std::cout << "Hope interpreter (C++20 rewrite)\n";
    std::cout << "Phase 1 in progress — lexer available, parser not yet written.\n";
    return 0;
}
